import React from 'react';
import Search from './Search';

const ShowSearch = () => {
    return (
        <Search />
    );
}
const BgChange = () => {
    return (
        <Search />
    );
}
const NavHandle = {
    ShowSearch, BgChange
}
export default NavHandle;